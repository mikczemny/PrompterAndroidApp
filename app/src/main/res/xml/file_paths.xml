<?xml version="1.0" encoding="utf-8"?>
<paths>
    <!-- The app's own recordings folder, shared read-only via FileProvider so a
         saved video can be opened in the system player. -->
    <external-files-path name="recordings" path="recordings/" />
</paths>
