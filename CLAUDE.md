# CLAUDE.md

This repository's canonical agent instructions are in `AGENTS.md`. Follow that
file in full. It supersedes any parent-directory instructions about unrelated
projects.

Key reminders: this is the proprietary Android Prompter app; preserve unrelated
work, check the Git remote at session start, keep `match/` Android-free, marshal
speech callbacks to the main thread, never commit signing credentials, and run
the verification commands listed in `AGENTS.md` before handing off changes.
