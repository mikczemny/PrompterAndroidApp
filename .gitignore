# Android / Gradle
*.iml
.gradle/
/local.properties
.idea/
.claude/
.DS_Store
build/
/captures
.externalNativeBuild
.cxx
*.apk
*.aab
*.keystore
*.jks

# Upload-signing credentials — never commit the keystore or its passwords.
/keystore.properties

# Reference prototype (kept locally, not part of the app build)
/src_extracted/
